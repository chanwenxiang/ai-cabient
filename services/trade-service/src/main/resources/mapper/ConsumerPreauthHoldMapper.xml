<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "https://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.aicabinet.trade.mapper.ConsumerPreauthHoldMapper">

  <select id="findByIdForUpdateRaw" resultType="com.aicabinet.trade.domain.ConsumerPreauthHold">
    <![CDATA[
    SELECT h.* FROM consumer_preauth_hold h WHERE h.session_id = #{sessionId} FOR UPDATE
    ]]>
  </select>
</mapper>
