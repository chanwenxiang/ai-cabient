<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "https://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.aicabinet.trade.mapper.OpsOrgNodeMapper">

  <select id="_findByIdForUpdateRaw" resultType="com.aicabinet.trade.domain.OpsOrgNode">
    <![CDATA[
    SELECT n.* FROM ops_org_node n WHERE n.node_id = #{nodeId} FOR UPDATE
    ]]>
  </select>

</mapper>
