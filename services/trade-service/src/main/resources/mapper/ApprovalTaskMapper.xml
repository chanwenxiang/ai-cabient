<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "https://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.aicabinet.trade.mapper.ApprovalTaskMapper">

  <select id="findByIdForUpdateRaw" resultType="com.aicabinet.trade.domain.ApprovalTask">
    <![CDATA[
    SELECT * FROM approval_task WHERE task_id = #{taskId} FOR UPDATE
    ]]>
  </select>

</mapper>
