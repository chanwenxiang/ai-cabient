<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "https://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.aicabinet.trade.mapper.DeviceTemperatureReadingMapper">

  <select id="findByDeviceIdSince" resultType="com.aicabinet.trade.domain.DeviceTemperatureReading">
    <![CDATA[
    SELECT r.* FROM device_temperature_reading r
                        WHERE r.device_id = #{deviceId} AND r.reported_at >= #{since}
                        ORDER BY r.reported_at ASC
    ]]>
  </select>
</mapper>
