<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "https://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.aicabinet.trade.mapper.CabinetOrderMapper">

  <select id="sumTotalAmountSince" resultType="long">
    <![CDATA[
    SELECT COALESCE(SUM(o.total_amount_cents), 0) FROM cabinet_order o WHERE o.created_at >= #{since}
    ]]>
  </select>

  <select id="sumTotalAmount" resultType="long">
    <![CDATA[
    SELECT COALESCE(SUM(o.total_amount_cents), 0) FROM cabinet_order o
    ]]>
  </select>

  <select id="sumAmountByDeviceId" resultType="long">
    <![CDATA[
    SELECT COALESCE(SUM(o.total_amount_cents), 0) FROM cabinet_order o WHERE o.device_id = #{deviceId}
    ]]>
  </select>

  <select id="sumAmountByDeviceIdSince" resultType="long">
    <![CDATA[
    SELECT COALESCE(SUM(o.total_amount_cents), 0) FROM cabinet_order o
    WHERE o.device_id = #{deviceId} AND o.created_at >= #{since}
    ]]>
  </select>

  <select id="sumTotalAmountBetween" resultType="long">
    <![CDATA[
    SELECT COALESCE(SUM(o.total_amount_cents), 0) FROM cabinet_order o
    WHERE o.created_at >= #{start} AND o.created_at < #{end}
    ]]>
  </select>

  <select id="findOrderIdsBetween" resultType="string">
    <![CDATA[
    SELECT o.order_id FROM cabinet_order o WHERE o.created_at >= #{start} AND o.created_at < #{end}
    ]]>
  </select>

  <select id="findByCreatedAtBetween" resultType="com.aicabinet.trade.domain.CabinetOrder">
    <![CDATA[
    SELECT o.* FROM cabinet_order o WHERE o.created_at >= #{start} AND o.created_at < #{end}
    ]]>
  </select>

  <select id="sumTotalAmountByDeviceIdIn" resultType="long">
    SELECT COALESCE(SUM(o.total_amount_cents), 0) FROM cabinet_order o
    WHERE o.device_id IN
    <foreach collection="deviceIds" item="__item" open="(" separator="," close=")">#{__item}</foreach>
  </select>

  <select id="sumTotalAmountByDeviceIdInSince" resultType="long">
    SELECT COALESCE(SUM(o.total_amount_cents), 0) FROM cabinet_order o
    WHERE o.device_id IN
    <foreach collection="deviceIds" item="__item" open="(" separator="," close=")">#{__item}</foreach>
    AND o.created_at &gt;= #{since}
  </select>

  <select id="sumTotalAmountByDeviceIdInBetween" resultType="long">
    SELECT COALESCE(SUM(o.total_amount_cents), 0) FROM cabinet_order o
    WHERE o.device_id IN
    <foreach collection="deviceIds" item="__item" open="(" separator="," close=")">#{__item}</foreach>
    AND o.created_at &gt;= #{start} AND o.created_at &lt; #{end}
  </select>
</mapper>
