<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "https://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.aicabinet.trade.mapper.OpsRoleMapper">

  <select id="_findByIdForUpdateRaw" resultType="com.aicabinet.trade.domain.OpsRole">
    <![CDATA[
    SELECT r.* FROM ops_role r WHERE r.role_id = #{roleId} FOR UPDATE
    ]]>
  </select>

</mapper>
