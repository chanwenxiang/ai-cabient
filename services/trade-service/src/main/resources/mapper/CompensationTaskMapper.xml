<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "https://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.aicabinet.trade.mapper.CompensationTaskMapper">

  <select id="findExecutableTasks" resultType="com.aicabinet.trade.domain.CompensationTask">
    <![CDATA[
    SELECT c.* FROM compensation_task c
    WHERE c.status = 'PENDING' AND c.scheduled_at <= #{now}
    ORDER BY c.priority DESC, c.scheduled_at ASC
    LIMIT 200
    ]]>
  </select>
</mapper>
