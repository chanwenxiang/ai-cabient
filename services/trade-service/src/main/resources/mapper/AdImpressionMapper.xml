<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "https://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.aicabinet.trade.mapper.AdImpressionMapper">

  <select id="sumCostByCampaignId" resultType="java.math.BigDecimal">
    SELECT SUM(i.cost) FROM ad_impression i WHERE i.campaign_id = #{campaignId}
  </select>

  <select id="countByCampaignIdAndEventType" resultType="java.lang.Long">
    SELECT COUNT(*) FROM ad_impression i WHERE i.campaign_id = #{campaignId} AND i.event_type = #{eventType}
  </select>
</mapper>
