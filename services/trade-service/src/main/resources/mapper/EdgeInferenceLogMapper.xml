<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "https://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.aicabinet.trade.mapper.EdgeInferenceLogMapper">

  <select id="getAverageInferenceTime" resultType="java.lang.Double">
    <![CDATA[
    SELECT AVG(l.inference_time_ms) FROM edge_inference_log l WHERE l.device_id = #{deviceId} AND l.inference_at >= #{start}
    ]]>
  </select>

  <select id="countSuccessInferences" resultType="java.lang.Long">
    <![CDATA[
    SELECT COUNT(*) FROM edge_inference_log l WHERE l.device_id = #{deviceId} AND l.status = 'SUCCESS' AND l.inference_at >= #{start}
    ]]>
  </select>
</mapper>
