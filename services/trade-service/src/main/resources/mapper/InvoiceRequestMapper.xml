<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "https://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.aicabinet.trade.mapper.InvoiceRequestMapper">

  <select id="_findByIdForUpdateRaw" resultType="com.aicabinet.trade.domain.InvoiceRequest">
    <![CDATA[
    SELECT i.* FROM invoice_request i WHERE i.invoice_id = #{invoiceId} FOR UPDATE
    ]]>
  </select>

  <select id="_findActiveByOrderIdForUpdateRaw" resultType="com.aicabinet.trade.domain.InvoiceRequest">
    <![CDATA[
    SELECT i.* FROM invoice_request i
    WHERE i.order_id = #{orderId}
      AND i.status IN ('PENDING', 'ISSUED')
    ORDER BY i.invoice_id
    LIMIT 1
    FOR UPDATE
    ]]>
  </select>

</mapper>
