== 1/5 URL 形状校验 ==
  [PASS] 联调模式：放行 http://tmp-feishu-sink:18099（这不是飞书，只可用于本地替身）
  目标: http://tmp-feishu-sink:18099/…/hook/******send
  签名: 关（若机器人开了「签名校验」，飞书会拒收 code=19021）
== 2/5 写入 infra/.env（幂等，保留 CRLF）==
  - FEISHU_WEBHOOK_URL 已更新（第 85 行）
  - FEISHU_SIGN_SECRET 已是目标值（未改动）
  [PASS] 回读一致；文件 2822 → 2774 字节，换行仍为 CRLF
== 3/5 重建 relay 容器（--force-recreate）==
  [PASS] relay 已重建并起监听
== 4/5 复核容器内取值（防「改了 .env 没生效」）==
  [PASS] 容器内取值 == 目标值；签名密钥为空
== 5/5 真实投递自检（飞书业务码必须为 0）==
  [PASS] 已送达

== 结论：飞书告警渠道已接通，且实测送达 ==
  请到飞书群里确认是否收到那条自检消息（这是最后一道人工确认）。
