---
description: 开门柜 IoT/支付业务规范：分布式锁、幂等、状态机、硬件容错
alwaysApply: true
---

# 开门柜业务专属规范（IoT & Payment）

## 核心架构与并发

- 分布式锁：开门指令、支付回调、库存扣减禁止单机锁；用 Redisson/ZooKeeper
- Lock Key 必须含设备 ID 或订单号（如 `lock:door:open:{deviceId}`），并设合理过期防死锁
- 异步解耦：开门下发、硬件状态上报、消息推送等非核心耗时操作走 MQ（RabbitMQ/Kafka），禁止主线程同步阻塞
- 幂等：资金与状态变更接口必须用 DB 唯一索引或 Redis Token，防重试导致重复开门/扣款

## 安全与支付

- PII 脱敏：手机号、支付账号、人脸特征值等在日志与接口返回中脱敏
- 禁止硬编码密钥/密码；从配置中心或环境变量读取
- 状态机：开门柜状态（空闲 → 扫码鉴权 → 开门中 → 使用中 → 结算中）用状态机管理
- 禁止用随意 `if-else` 改状态；每次变更必须记操作流水

## 硬件交互与容错

- 与硬件网关通信必须设严格 Timeout
- 网络抖动失败须带退避（Backoff）的重试
- 硬件异常（如门未关好）须有明确捕获、兜底与告警；禁止未处理异常打断主流程
