---
description: 运营后台主区防抖硬约束（svh / 禁止 visualViewport / 勿 translateZ / dock 取整）
globs: clients/admin-vue/**/*.{vue,ts,css}
alwaysApply: false
---

# 运营后台布局防抖（禁止回归）

历史多次复现：鼠标上下移动时主区抖 1～2px。改布局、表格滚动、浮动横滑条前必须遵守下列硬约束。

## 必须

1. **视口高度用 `100svh`（可回退 `100vh`）**，禁止 `100dvh`。  
   `dvh` 会随浏览器栏/任务栏显隐变化，鼠标移到上下边缘时整页抖。
2. **`table-scroll-fit` 只观察 `childList`**，禁止监听 `attributes` / class。  
   EP 行悬停会狂写 `hover-row`，听 class 会反复测宽 + reflow。
3. **忽略表体行增删**（`tbody` / `.el-table__body*`），轮询刷新数据也不要触发测宽。
4. **只跟 `window.resize`**。**禁止任何 `visualViewport` 监听**（含 `resize` / `scroll`）。  
   桌面端鼠标移到上下边缘时 vv 会误触发 → 测宽/写浮动横滑条 → 主区抖。
5. **主区勿用 `transform: translateZ(0)` / 强开合成层**；偶发亚像素错位，刷新才消，像「随机跳动」。
6. 主滚动容器保持：`overflow-y: scroll` + `scrollbar-gutter: stable` + `overflow-anchor: none`。
7. **浮动横滑条几何必须取整 + ≥2px 回滞**，滚动期合并更新（约 100ms）；值未变不要写 `style`。
8. **`.table-scroll` 内 EP 左右 sticky 固定列一律 `position: static`**。  
   右侧 sticky 操作列在缩放 / 非 1× DPR 下会亚像素错位并整页微抖；横滑用外壳即可。
9. 壳层高度用整数 `--layout-vh-px`（`Math.round(innerHeight)`）覆盖 `svh`，避免 `882.767px` 这类亚像素高度。
10. **侧栏与主区交界勿留浅色竖条**：侧栏原生滚动条必须 `scrollbar-width: none`（全局细条样式勿盖掉侧栏隐藏）。主区保持左右对称 padding，勿为消竖条清零 `padding-left`。

## 关键文件

- `clients/admin-vue/src/layouts/AdminLayout.vue`
- `clients/admin-vue/src/utils/table-scroll-fit.ts`
- `clients/admin-vue/src/styles/main.css`

## 改完自检

- 源码与构建产物中：**有 `100svh` / `--layout-vh-px`、无 `100dvh`**；**无** `visualViewport` 监听；**无** `translateZ(0)`；**无** `.table-scroll` 内右侧 `sticky`。
- 设备/订单等列表页：上下移鼠标时主区 `scrollTop` / `getBoundingClientRect().top` 不应跳变。

## 禁止借口

「浮动横滑条 / 缩放适配需要听 visualViewport」「dvh 更跟手」「translateZ 能减轻重绘」「右侧操作列必须 sticky」——均已验证会抖或偶发坏状态；窗口尺寸只听 `window.resize`；操作列跟表一起横滑。
