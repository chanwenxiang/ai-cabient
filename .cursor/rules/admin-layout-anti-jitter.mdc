---
description: 运营后台布局防抖与审单工作台硬约束（操作列 sticky、抽屉拖宽、滚动条、描述表、调整明细）
globs: clients/admin-vue/**/*.{vue,ts,css}
alwaysApply: false
---

# 运营后台布局防抖 / 审单工作台（禁止回归）

改 `AdminLayout` / `table-scroll-fit` / `main.css` / `ResizableDrawer` / `useResizableDrawer` /
`ExceptionListView` / `DisputeListView` 前必读。合入前跑：`pnpm check:admin-anti-jitter`。

## 问题 → 根因 → 解决方案（本轮踩过的坑）

| # | 现象 | 根因 | 必须怎么做 |
|---|------|------|------------|
| A | 操作列横滑时跑走 | 为「防抖」把右侧 `sticky` 改成 `static` | **右侧必须** `.table-scroll … fixed-column--right { position: sticky; right: 0 }` + 不透明底；左侧 fixed 仍可 `static` |
| B | 抽屉拉窄松手后自动变宽 | 松手先清 `style.width`，EP 短暂用回旧 `:size` | 先 `style.width=finalW` + `width.value=finalW` → `nextTick` 再去 `.is-resizing`；**禁止** pointerup 清空 inline width |
| C | 拖抽屉时下半区（调整明细）抖 | 每帧写 Vue `width` / 缺回滞 / 内部重排 | 拖动只改 DOM；`Math.round` + ≥2px 回滞；`.is-resizing` 冻结 transition / contain |
| D | 单点抽屉纵向滚动条：内容往左缩再弹回；按钮折行闪 | Win 叠加滚动条 overlay→经典条吃宽；`overflow-y:auto` + `flex-wrap:wrap` | body：`overflow-y: **scroll**`（禁 auto）+ `scrollbar-gutter: stable` + `overflow-anchor: none`；`.workbench-media-actions`：`flex-wrap: **nowrap**`（过窄横滑） |
| E | 描述表「异常类型/关联设备」等盖住邻格标签 | 窄格 + Tag/`overflow:visible` 溢出绘制 | 标签 `nowrap`；**内容**格内换行 + `overflow:hidden`；`.el-tag`/`link-cell` `max-width:100%` + `white-space:normal` |
| F | 调整明细数量框挡住「删除」 | grid 数量列 132px < EP `input-number`（右控件）默认 ~150px | `.manual-line`：`… 150px max-content`；`.el-input-number { width/max-width:100% }` |
| H | 长文案悬停浮层盖住邻列/操作列 | `show-overflow-tooltip` 的 EP 浮层挂在表内，横滚+sticky 时错位 | **全后台禁止** `show-overflow-tooltip`；全局 `installTableCellNativeTitle`；可选手写 `.cell-ellipsis` + `:title` |

## 主区硬约束

1. 视口高度：`100svh`（可回退 `100vh`），**禁止 `100dvh`**。  
2. `table-scroll-fit` 只观察 `childList`，禁止 `attributes` / class。  
3. 忽略表体行增删（`tbody` / `.el-table__body*`）。  
4. 只跟 `window.resize`；**禁止任何 `visualViewport` 监听**。  
5. 主区勿用 `transform: translateZ(0)`。  
6. 主滚动：`overflow-y: scroll` + `scrollbar-gutter: stable` + `overflow-anchor: none`。  
7. 浮动横滑条几何取整 + ≥2px 回滞。  
8. 操作列右侧 sticky（见上表 A）；壳层高度整数 `--layout-vh-px`；侧栏滚动条隐藏。

## 审单抽屉（ResizableDrawer）

9. 拖动中禁止每帧写 Vue `width`：只改 `drawerEl.style.width`。  
10. 宽度 `Math.round` + ≥2px 回滞。  
11. `.is-resizing`：禁 transition、body `overflow:hidden` + `contain: layout style`。  
12. 松手顺序见上表 B；抽屉面板可 `transition: none`（避免 EP 宽度动画像「自己变宽」）。  
13. 抽屉 body 见上表 D（`scroll` + gutter）。

## 审单工作台内容区（`.drawer-workbench`）

14. `.workbench-grid`：`minmax(0, 1fr) minmax(0, 1.15fr)`，**禁止** `minmax(300px/340px, …)` 硬下限（挤窄时单字竖排/叠层）。  
15. `.workbench-media-actions`：`flex-wrap: nowrap`（见上表 D）。  
16. `.workbench-desc`：标签不换行；内容可换行且不溢出盖邻格（见上表 E）。  
18. 列表长文案：**全后台禁止** `show-overflow-tooltip`（门禁扫全部 `clients/admin-vue/src/**/*.vue`）；截断全文靠 `installTableCellNativeTitle` 或 `.cell-ellipsis` + `:title`（见上表 H）。

## 关键文件

- `clients/admin-vue/src/styles/main.css`  
- `clients/admin-vue/src/components/ResizableDrawer.vue`  
- `clients/admin-vue/src/composables/useResizableDrawer.ts`  
- `clients/admin-vue/src/views/exceptions/ExceptionListView.vue`  
- `clients/admin-vue/src/views/disputes/DisputeListView.vue`  
- 门禁：`scripts/check-admin-anti-jitter.mjs` → `pnpm check:admin-anti-jitter`

## 改完自检（有 UI 时用 Playwright）

- [ ] 列表横滑：操作列 sticky 钉在右侧  
- [ ] 抽屉拉窄松手：宽度保持，不弹回更宽  
- [ ] 单点抽屉纵向滚动条：内容宽不闪、按钮不闪折行  
- [ ] 描述表：邻格标签不被 Tag/长文案盖住；内容可格内换行  
- [ ] 调整明细：数量框与「删除」无重叠（间距 ≥ gap）  
- [ ] 无 `100dvh` / `visualViewport` / `translateZ(0)`

## 禁止借口

| 借口 | 结论 |
|------|------|
| 「为防抖关掉操作列 sticky」 | **否**。产品要锁住；只禁左侧 sticky。 |
| 「松手清掉 inline 更干净」 | **否**。须 nextTick；或保留 inline=finalW。 |
| 「dvh / visualViewport 更跟手」 | 否；主区抖。 |
| 「overflow-y:auto 够用了」 | 否；点滚动条会挤内容（表 D）。 |
| 「按钮 wrap 一下也行」 | 否；滚动条闪动会闪行；用 nowrap。 |
| 「数量列再窄一点省地方」 | 否；小于 150px 会盖「删除」。 |
| 「用 show-overflow-tooltip 看全文」 | 否；浮层会盖邻列。全局原生 title / cell-ellipsis + title。 |
