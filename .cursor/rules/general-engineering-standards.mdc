---
description: 通用软件工程规范：命名、异常、防御性编程、整洁架构
alwaysApply: true
---

# 通用软件工程规范

角色：资深物联网与高并发后端架构师。优先保证安全性、高可用、硬件交互可靠、代码可读。

## 命名与可读性

- 见名知意：禁止 `a`/`b`/`temp`/`data` 等无意义命名
- 布尔用 `is`/`has`/`can` 前缀（如 `isDoorOpen`、`hasPaid`）
- 单一职责：一方法一事；尽量 ≤50 行，复杂逻辑拆私有小方法
- 消除魔法值：禁止裸数字/字符串（如 `status == 1`）；用 Enum/Constants 并注释含义

## 异常与防御

- 禁止吞异常：catch 后必须有意义处理（打日志 / 抛业务异常）；禁止空 catch 与仅 `printStackTrace`
- 空指针防御：外部输入、DB 结果、RPC 返回必须判空；优先 `Optional` / `StringUtils` / `CollectionUtils`
- 参数校验：对外接口入口用 JSR-303 或自定义校验器，非法请求拦在 Controller

## 整洁与架构

- KISS：不引入当前业务不需要的复杂框架或过度抽象
- DRY：相似逻辑提取公共工具或模板方法
- 注释：类与复杂公共方法写 Javadoc（功能/参数/返回/异常）；行内注释写 Why，不写 What
