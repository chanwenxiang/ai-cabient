---
description: 前台用户可见文案统一中文（管理后台 / H5 / 小程序）
globs: clients/**/*.{vue,ts,tsx,js},packages/shared-uni/**/*.{ts,js},packages/shared-dict/**/*.{ts,js}
alwaysApply: false
---

# 前台展示文案：必须中文

面向运营、商户、消费者的 **UI 文案一律使用中文**，不要把英文技术词直接露给用户。

## 必须中文

- 按钮、标题、菜单、侧栏、表头、空态、占位符、Toast / Message / MessageBox
- 表单校验提示、二次确认文案、权限不足提示、加载失败提示
- 业务状态展示（优先字典中文标签；无字典时手写中文，勿直接展示 `PENDING` / `MISMATCH` 等裸枚举）

## 可以保留英文/符号

- 代码标识、权限码、路由 path、API 字段名、日志、注释
- 专有名词缩写在中文语境中的惯用写法（如 SKU、OTA、HTTP 状态码旁注）
- 用户输入的原文（商户名、备注等）按原样展示

## 反例 → 正例

- `Load failed` → `加载失败`
- `No data` → `暂无数据`
- `Confirm` → `确认`
- 毛利率无营收显示 `0.0%` 且像真实 0 → 用 `暂无` / `—` 区分
