---
description: 项目踩坑必须记「现象→根因→必须怎么做」，避免同类问题反复发生
alwaysApply: true
---

# 踩坑记录（强制）

修完**非显而易见**或**易复发**的 bug / 布局 / 联调问题后，**必须**留下「现象 → 根因 → 必须怎么做」，禁止只改代码不留痕。

## 何时记

满足任一即记：

- 用户投诉或验收复现的问题（尤其 UI 抖、盖字、弹回、空态误解）
- 修了两次以上、或靠猜测易再改错的逻辑
- 与框架默认行为冲突（EP tooltip、滚动条、sticky、`:size` 等）
- 业务空态易被当成「丢数据」

纯笔误、一次性 typo 可不记。

## 怎么记（固定三列）

| 现象 | 根因 | 必须怎么做 |
|------|------|------------|
| 用户可见的表现（一句话） | 技术原因（一句话，禁空话） | 可执行约束：禁止/必须写什么、跑什么门禁 |

可选第四列：相关文件 / 门禁命令。

## 记到哪里（按优先级）

1. **已有领域规则表**：能归入则追加一行（例：后台布局 → `admin-layout-anti-jitter.mdc`）。
2. **总册**：否则追加到 `docs/engineering/lessons-learned.md`。
3. **可机器检查的**：同步改 `scripts/check-*.mjs` 或现有 `pnpm check:*`，避免只靠文档。
4. **活文档**：`docs/PROJECT_KNOWLEDGE.md` §9 Changelog 追加一行（索引，不复制整表）。
5. **可复用流程**：执行 Skill `encapsulate-solved-problem`（更新 `solved-problems-playbook` 或新建 Skill）。

## 修完自检

- [ ] 三列表格已写入规则或总册  
- [ ] 活文档 Changelog 已追加  
- [ ] 若能量化，门禁/脚本已加（并进 CI）  
- [ ] 高频配方已进 `solved-problems-playbook`（或新 Skill）  
- [ ] 回复用户时可一句话指向「已写入某某规则/Skill，防再犯」

## 禁止

- 只说「已修好」不写根因与硬约束  
- 用「可能是 / 应该是」当根因（须有实测或代码证据）  
- 把领域细则只写在聊天里、不落仓库
