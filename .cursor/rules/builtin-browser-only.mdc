---
description: UI 验证与浏览器操作必须使用 Cursor 内置浏览器，禁止用 headless Playwright/脚本冒充验收
alwaysApply: true
---

# 内置浏览器验收（强制）

用户明确要求：**以后所有验证和对浏览器的操作，都必须在 Cursor 内置浏览器中进行。**

## 必须

1. 验收 / 冒烟 / 联调 / 回归涉及 UI 时，只用 **Cursor IDE Browser MCP**（`cursor-ide-browser`：`browser_navigate` → `browser_snapshot` → `browser_lock` → 操作 → `browser_unlock`）。
2. 操作要落在用户能看见的内置浏览器标签页里（如 Simple Browser / IDE Browser），不要另开不可见的 headless 会话当作「已验收」。
3. 宣称通过前：真实打开页面、完成关键步骤（登录、点击、填表、跳转），并说明实际看到的结果。

## 禁止

- 用 headless Playwright / Puppeteer / 本地 node 脚本登录点页面，然后对外报告「浏览器验证通过」。
- 仅凭 curl、API、日志或代码推理声称 UI 通过（有内置浏览器可用时）。
- 在用户正看着登录空页时，声称已经在「该页面」完成过操作。

## 例外

- 纯后端、无 UI 的接口：可用 curl/脚本，但仍优先 health/actuator。
- 内置浏览器 MCP **未启用/不可用**时：先告知用户并请其启用 `cursor-ide-browser`；**不得**改用 headless 冒充验收，除非用户当场明确允许临时例外。
