---
description: AI Cabinet monorepo overview and production-quality defaults
alwaysApply: true
---

# AI Cabinet 核心约定

- 生产标准写代码（鉴权、内部 API、权限隔离）；本地默认 `dev` + mock，不要为图省事关掉安全边界。
- 模块：`clients/admin-vue`、`clients/consumer-mp`、`clients/merchant-mp`、`packages/shared-*`、`services/trade-service`、`services/device-service`、`services/common/common-core`、`vision-service`、`infra/`。
- 对外 HTTP 多为 `/api/v2/...`；服务间 `/internal/v1/...`，请求头 `X-Internal-Api-Key`。
- 统一响应：`com.aicabinet.common.dto.ApiResponse`（`code=0` 成功）与 `PageResult`；DTO 放 `common-core`。
- DB：PostgreSQL（默认 `localhost:15433/aicabinet`），schema 变更只用 Flyway `V{n}__*.sql`（当前已到 V125+）。
- 改 API 时同步：`common-core` DTO、调用方、必要时 `packages/shared-types`；不要另起一套类型体系。
- 禁止提交密钥；配置用环境变量（`INTERNAL_API_KEY`、`JWT_SECRET`、`VISION_API_KEY` 等）。
