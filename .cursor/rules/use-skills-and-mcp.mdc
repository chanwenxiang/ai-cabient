---
description: 写代码与测试前必须先匹配并使用可用 Skill / MCP
alwaysApply: true
---

# 写代码 / 测试前优先使用 Skill 与 MCP

在**编写、修改、调试、测试**代码时，先做能力匹配，再动手；不要只靠记忆或猜测。

## 1. 开工前必检（每次相关任务）

1. **Skills**：先看本仓库 `.cursor/skills/`（含 `ai-cabinet-dev-test` 路由），再看会话 `available_skills` 与 `~/.cursor/skills/`。按任务关键词匹配 `description`，命中则先 `Read` 其 `SKILL.md` 并严格按步骤执行。
2. **MCP**：查看当前已启用 MCP 与工具。能用工具完成的（查文档、查库、浏览器验证、错误追踪等）优先走 MCP，不要用猜测替代。
3. **本仓快捷入口**：涉及写代码 / 测试 / 联调时，优先 `Read` `.cursor/skills/ai-cabinet-dev-test/SKILL.md`。

## 2. 常见匹配（示例，非穷尽）

| 场景 | 优先 |
|------|------|
| **本仓开发/测试总路由** | `ai-cabinet-dev-test`（`.cursor/skills/`） |
| **安全扫描** | `security-scan` → `security-best-practices` |
| **代码质量审查** | `code-review` |
| **UI 规范/a11y** | `web-design-guidelines` |
| 库/框架 API、版本差异、用法不确定 | Context7（`resolve-library-id` → `query-docs`） |
| UI / 页面 / 联调 / 验收 / 冒烟（有条件必测，见 browser-real-testing） | **优先 Playwright MCP / CLI**；不可用再回退 Cursor 内置 Browser |
| 生产/线上报错、issue、trace | Sentry MCP 或 `sentry` skill |
| DB schema / 数据核对 | Postgres 等数据库 MCP |
| 前端视觉与页面设计 | `frontend-design` + `ui-implementation` skill |
| 复杂功能规划 / TDD / 调试流程 | Superpowers（如 `brainstorming`、`test-driven-development`、`systematic-debugging`） |
| 安全分析 / 威胁建模 / 合规 | Cybersecurity skills（按名称点名加载） |
| Sanity / CMS | Sanity MCP + 对应 sanity skills |
| GitHub PR/Issue | GitHub MCP |

## 3. 执行顺序

```
匹配 Skill/MCP → 阅读 Skill 或调用 MCP → 再写/改代码 → 用 MCP 做验证（尤其 UI）
```

- 有多个命中时：先读 skill 约束流程，再调 MCP 取事实，最后改代码。
- Skill 与项目规则冲突时：以**项目规则 / 用户明确指令**为准，并在回复中简要说明取舍。
- 没有可用 Skill/MCP 时再自行实现；不要为了“用工具”硬调无关 MCP。

## 4. 禁止

- 未检查可用 Skill/MCP 就开始大段实现或宣称测试通过
- 仅用 curl/日志推断 UI 通过（必须用 Playwright 或回退的内置 Browser）
- 对库 API 凭训练记忆下结论（有 Context7 时）
- Playwright 可用时仍故意跳过浏览器验收
