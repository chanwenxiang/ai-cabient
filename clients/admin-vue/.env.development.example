# 复制为 .env.development.local（本机自用，.gitignore 已忽略）后按需填写。
#
# 🔴 不要建 .env.local：它在**所有 mode** 下被 vite 加载，会把下面的 key 字面量
#    内联进 `vite build`（production）产物，而 CI 检出里没有这个文件
#    ⇒ 同一提交在本机与 CI 构建出不同 chunk 哈希，admin-artifacts 逐字节比对必红。
#    只用 development 档 ⇒ dev 带 key 走高德，production 产物保持一致（降级 Leaflet）。

# dev server(:3000) 代理 /api 到后端时会回显此 Origin；不填则后端
# DefaultCorsProcessor 以 403 拒绝该代理请求。（与 vite.config.ts 的 fallback 一致）
VITE_DEV_ORIGIN=http://localhost

# 高德 JS API key（控制台「Web端(JS API)」类型）：大屏地图用官方暗色底图。
# 留空则大屏降级为 Leaflet 免 key 瓦片 —— 生产构建就是这个状态。
VITE_AMAP_JS_KEY=
# 上述 key 配套的安全密钥（控制台同名条目；JS API 加载前注入 _AMapSecurityConfig）
VITE_AMAP_SECURITY_CODE=
